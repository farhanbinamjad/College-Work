NOTE:  Before you can compile (nvcc) the CUDA programs you need to 
load the CUDA module by issuing the following command at the shell prompt:

module load cudatoolkit

You'll need to issue this command each time you log-on to Blue Waters and want
to use the CUDA compiler.

